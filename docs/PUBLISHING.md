# Publishing notes

Before public release, verify:

- no local-only artifacts are tracked
- `.env` is ignored
- README reflects actual validation commands
- local rebuild is deterministic
- policy metadata and live-check output still match the documented demo

This repo is designed to be a companion entrypoint for developers evaluating Forte on NFTs.
